# Документация на bom_classifier.html

## Обща информация
**Име на файла:** `bom_classifier.html`  
**Предназначение:** Интерактивен класификатор на детайли и части - Производство (Цех) или Закупуване (Външно сглобяване)

---

## Основни функции

### 1. Зареждане на BOM файлове
- Поддържа **JSON** и **CSV** формати
- Парсване на различни структури:
  - **Enhanced CSV** - с допълнителни колони (тип, файлов път, изображение, детайли, подасемблита)
  - **Standard CSV** - стандартна BOM структура
  - **JSON v2.0** - пълен формат с items масив
  - **JSON v1.0** - стара версия само с assemblies

#### Функция `parseCSVToBOMData()`
Парсира CSV файл и конвертира в BOM структура:
- Премахва BOM marker (`\ufeff`)
- Обработва escaped кавички
- Разпознава основно асембли (depth = 0)
- Извлича детайли и подасемблита

### 2. Обобщена таблица на асемблитата
След зареждане на BOM файла се показва:
- Таблица с всички асемблита
- Колони:
  - **Категория** (със зелен фон - "ЦЕХ")
  - **№** - пореден номер
  - **Име на сборката** - с бутон ➕ за разгръщане
  - **Брой** - количество
  - **✓** - чекмарк за завършено
- Бутон **"🚀 Започни класификация на асемблитата"**

### 3. Интерактивна класификация

#### Показване на главна сборка
- Дисплей с:
  - Изображение на главната сборка
  - Име на сборката
  - Прогрес: обработени / общо асемблита
  - Процент на завършеност (с цветова индикация)

#### Процес на класификация
За всяка сборка се показва:
- **Име на сборката** (преведено на български)
- **Път към сборката**
- **Изображение** (ако има)
- **Обобщена статистика:**
  - Общо детайли
  - Подасемблита
- **Списък на детайлите** (със sortиране)
- **Списък на подасемблитата** (със sortиране)

#### Бутони за класификация
- ✅ **ДА - Сглобява се в цеха** (workshop)
- ❌ **НЕ - Сглобява се на обекта** (external)

#### Keyboard shortcuts
- `1`, `y`, `Y` → Класифицира като "Цех"
- `2`, `n`, `N` → Класифицира като "Външно"

### 4. Автоматична класификация
- Проверява родителски сборки
- Ако родител е класифициран за "Цех", автоматично класифицира и децата
- Показва notification с брой автоматично класифицирани подсборки
- Визуална индикация с ⚡ икона

#### Функция `checkAutoClassification()`
```javascript
// Проверява дали има родител вече класифициран
if (workshopPaths.has(parentPath)) {
    return 'workshop';
}
```

#### Функция `getInheritanceStatus()`
Показва съобщение: "Автоматично: родителят \"X\" е в цеха"

### 5. Визуализация на резултатите

#### В реално време (панел отдясно)
- **🏭 Сглобява се в цеха** - списък с цехови сборки
- **🏗️ Сглобява се на обекта** - списък с външни сборки
- Брой на класифицираните елементи
- Визуална индикация за автоматично класифицирани (жълта рамка, градиент фон)

#### Финален отчет
Структура на отчета:
1. **Заглавие и лого** (TREZ)
2. **Визуална секция:**
   - Име на главната сборка
   - Изображение
   - Номер на обект
3. **Класификационна таблица:**
   - Категория (ЦЕХ / ВЪНШНО)
   - Номер
   - Име на сборката
   - Брой
   - Чекмарк ✓
4. **Детайли за цехово сглобяване** - таблица с:
   - №
   - Име на детайла
   - Брой
5. **Детайли за външно сглобяване** - аналогична таблица

### 6. Експорт и запазване

#### Експорт на резултати (`exportResults()`)
- JSON файл: `bom_classification_results.json`
- Съдържа:
  - Timestamp
  - Summary (total, workshop, external)
  - workshopAssemblies[]
  - externalAssemblies[]

#### Експорт на подробни резултати (`exportDetailedResults()`)
- JSON файл: `detailed_bom_classification_report.json`
- Съдържа:
  - Metadata (mainAssembly, totalAssemblies, totalParts)
  - Classification (с partsCount и subAssembliesCount)
  - Summary (workshopParts, externalParts и т.н.)

#### Запазване на класификациите (`saveClassifications()`)
- Записва класификациите обратно в JSON файла
- Добавя към всяко асембли:
  - `classification` - "workshop" или "external"
  - `classificationDate` - ISO timestamp
- Добавя метаданни:
  - `classifiedBy`, `classificationDate`, `version`
  - `totalClassified`, `workshopCount`, `externalCount`
- Запазва глобални RAL настройки
- Изход: `{mainAssembly}_classified.json`

#### Принтиране на отчета (`printReport()`)
- Отваря нов прозорец с форматиран HTML
- Оптимизиран за печат (A4, margin: 10mm)
- Скрива бутоните при печат

### 7. Превод на имена

#### Речник `translationDictionary`
Превежда от английски/немски на български:
- Komplekt → Комплект
- Bufer → Буфер
- Planka → Планка
- Anker → Анкер
- M4x16 → М4х16
- И други...

#### Функция `translateName()`
Използва регулярни изрази за замяна на цели думи

### 8. Сортиране на имена

#### Функция `sortByName()`
Интелигентно сортиране:
1. **Имена започващи с числа** идват първи
   - Сортирани числово (1, 2, 10, 20)
2. **Имена започващи с букви** идват след тях
   - Сортирани азбучно (A, B, C)
3. При еднакви числа/букви - сравнява целите имена

### 9. Обединяване на асемблита

#### Функция `unifyAssemblies()`
Премахва дублиращи се асемблита:
- Премахва `<1>`, `<2>` и т.н. от имената
- Обединява количествата
- Merge-ва parts и subAssemblies
- Използва `Map` за бързо търсене

```javascript
const cleanName = assembly.name.replace(/<\d+>$/, '').trim();
if (unifiedAssemblies.has(cleanName)) {
    existingAssembly.quantity += assembly.quantity || 1;
}
```

### 10. Работа с изображения

#### Tooltip при hover
- Показва малък preview на изображението при посочване с мишка
- Интелигентно позициониране (ляво/дясно/центрирано)
- Анимирано появяване

#### Функция `convertImagePath()`
Конвертира абсолютни пътища в относителни:
```javascript
// C:\...\Screenshots\Parts\30_02_S1.png
// → images/Parts/30_02_S1.png
```

#### Функция `tryLoadImage()`
Опитва да зареди изображение с fallback опции:
1. Оригинален път
2. Без `_1_`, `_2_` суфикси
3. С `_1_` суфикс

#### Modal за изображения
- Кликване на име на част → показва голямо изображение
- Затваря се с:
  - Бутон ✕
  - Кликване извън модала
  - ESC клавиш

### 11. RAL цветови настройки

#### Глобални RAL настройки
- Modal прозорец с dropdown-и за RAL, RAL1-RAL9
- Предефинирани опции:
  - RAL: RAL 9005, RAL 5009, RAL 3020
  - RAL1: RAL 3020, Zn
  - RAL2: RAL 9010, Zn
  - И т.н.
- Запазват се в `globalRalSettings` обект
- Включват се в експортирания JSON файл

#### Функции
```javascript
openColorModal()    // Отваря модал за настройки
closeColorModal()   // Затваря модал
saveGlobalRalSettings()  // Запазва настройките
loadGlobalRalSettings()  // Зарежда настройките
```

---

## Дизайн и стилове

### Цветова схема
- Градиент фон: `#667eea` → `#764ba2`
- Хедър: `#2c3e50` → `#34495e`
- Бутон "Да" (цех): `#28a745` → `#20c997` (зелено)
- Бутон "Не" (външно): `#dc3545` → `#fd7e14` (червено)
- RAL/цветове: `#6f42c1` → `#5a32a3` (лилаво)

### Градиенти и ефекти
- Хедър: `linear-gradient(135deg, #2c3e50, #34495e)`
- Бутони: `box-shadow: 0 3px 10px rgba(0,0,0,0.2)`
- Hover ефект: `transform: translateY(-2px)`
- Прогрес бар: `linear-gradient(90deg, #28a745, #20c997)`

### Responsive дизайн
- Flex layout за основна структура
- Grid за детайли и статистики
- Auto-fit колони за RAL опции

---

## Ключови променливи

```javascript
let bomData = [];              // Масив с асемблита
let allItemsData = [];         // Всички елементи включително части
let currentIndex = 0;          // Текущ индекс в процеса
let workshopItems = [];        // Цехови сборки
let externalItems = [];        // Външни сборки
let workshopPaths = new Set(); // Пътища на цехови сборки
let externalPaths = new Set(); // Пътища на външни сборки
let originalJsonData = null;   // Оригинални JSON данни
let globalRalSettings = {};    // Глобални RAL настройки
```

---

## Работен процес

### Основен процес

1. **Зареждане на файл**
   ```
   Потребител избира файл (JSON/CSV)
   ↓
   loadBOM()
   ↓
   Парсване (parseCSVToBOMData или JSON.parse)
   ↓
   unifyAssemblies() - обединяване
   ↓
   setupMainAssemblyDisplay() - показване на главна сборка
   ↓
   showSummaryTable() - обобщена таблица
   ```

2. **Класификация**
   ```
   Кликване на "🚀 Започни класификация"
   ↓
   startClassification()
   ↓
   showCurrentItem() - показва текущо асембли
   ↓
   Потребител класифицира (ДА/НЕ)
   ↓
   classifyItem(isWorkshop)
   ↓
   Повтаря се до края
   ↓
   showCompleted()
   ```

3. **Експорт**
   ```
   Генериране на финален отчет
   ↓
   generateReport()
   ↓
   generateClassificationTable()
   ↓
   generatePartsTable()
   ↓
   Принтиране или експорт в JSON
   ```

---

## Особености и важни детайли

### Автоматична класификация
- Наследяване на категория от родител
- Пропуска се ръчната класификация
- Показва notification с брой автоматично класифицирани

### Обединяване на части
При генериране на таблици с детайли:
```javascript
const unifiedParts = new Map();
parts.forEach(part => {
    const cleanName = part.name.replace(/<\d+>/g, '').trim();
    if (unifiedParts.has(cleanName)) {
        unifiedParts.get(cleanName).quantity += part.quantity;
    } else {
        unifiedParts.set(cleanName, { name: cleanName, quantity: part.quantity });
    }
});
```

### Сортиране преди показване
Всички списъци се сортират с `sortByName()` за консистентност

### Визуални индикатори
- 📋 - BOM
- 🔧 - Детайли
- 🏗️ - Подасемблита
- 🏭 - Цехово сглобяване
- 🚚 - Външно сглобяване
- ⚡ - Автоматична класификация
- 🎨 - RAL цветове

---

## Интеграция с други системи

### Експорт формати
1. **Simple JSON** - основна класификация
2. **Detailed JSON** - с метаданни и статистики
3. **Classified JSON** - оригинални данни + класификации
4. **Print HTML** - форматиран за печат

### API endpoints (за бъдещи версии)
- Възможност за интеграция с API за автоматично зареждане
- Съвместимост с unified_bom_viewer.html чрез споделен JSON формат

---

## Заключение
`bom_classifier.html` е мощен интерактивен инструмент за класификация на BOM асемблита и части. Поддържа интелигентно сортиране, автоматична класификация, богата визуализация и множество формати за експорт. Идеален за производствени процеси, където е необходимо разделяне на части между цех и външно сглобяване.
