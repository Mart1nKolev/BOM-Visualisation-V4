# Документация на unified_bom_viewer.html

## Обща информация
**Име на файла:** `unified_bom_viewer.html`  
**Версия:** v2025-11-05B  
**Предназначение:** Визуализатор за обединени BOM (Bill of Materials) спецификации от множество обекти

---

## Основни функции

### 1. Автоматично зареждане на данни
- При стартиране файлът автоматично извлича BOM данни от endpoint `/get_bom`
- Поддържа два режима на работа:
  - **Мрежов режим** (споделен режим) - данните се синхронизират между устройства
  - **Локален режим** - данните се запазват само на устройството в localStorage

### 2. Визуализация в таб-базирана структура

#### Таб "📋 Преглед" (Overview)
- Показва обобщена информация за всички възли/обекти
- Карти с ключови метрики за всеки обект:
  - Брой детайли (🔧)
  - Брой крепежи (⚙️)
  - RAL кодове за всеки възел

#### Таб "🏢 По възли" (By Object)
- Филтриране на данни по конкретен възел/обект
- Показва всички елементи (assemblies) за избрания възел
- Таблица с колони: Ниво, Обект, Име, Път, Количество, RAL

#### Таб "📊 Всички части" (Parts Summary)
- Два режима на показване:
  - **Всички части** - глобално обобщение на всички части
  - **По възли** - разбивка по отделни обекти
- Таблица с checkbox-ове за отбелязване на завършени части
- Проследяване на количества и източници (обекти)

#### Таб "🔧 Детайли" (Details Parts)
- Показва само детайлите (не крепежи)
- Филтрация: "Всички детайли (общо)" или "По възли"
- Сортиране по числови префикси (напр. 04_09 → 04.09)
- Обобщени количества

#### Таб "⚙️ Крепежи" (Fastener Parts)
- Показва само крепежните части
- Филтрация: "Всички крепежи (общо)" или "По възли"
- Интелигентно сортиране:
  - Първо по тип (Bolt, Nut, Washer и т.н.)
  - После по размер на резба (M4 → M6 → M8 → M10 → M12)
  - Накрая азбучно
- Поддръжка на различни формати: M8x20, M10 x 40, Anker M10x80

#### Таб "📃 Всички елементи" (All Items)
- Показва всички елементи от flatBOM (включително части)
- Пълна таблица с всички детайли

#### Таб "🎨 RAL елементи" (RAL Items)
- Управление на RAL цветови кодове
- Таблица за добавяне/редактиране на RAL записи:
  - RAL код (RAL, RAL1, RAL2... RAL9)
  - Описание на възела
  - Избрана опция (dropdown с предефинирани цветове)
  - Описание на цвета
- Функции:
  - ➕ Добави нов RAL
  - 💾 Запази всички
  - 🗑️ Изтрий запис
- Показва всички елементи с RAL кодове

### 3. Споделен режим (Network Mode)
- Автоматично откриване на мрежов сървър
- Индикатор за статус на връзката (горе-дясно)
- Синхронизация на потребителски данни между устройства:
  - Checkbox състояния
  - Бележки към части
  - RAL настройки

### 4. Управление на потребителски данни

#### Checkbox функционалност
- Отбелязване на завършени/обработени части
- Визуални ефекти:
  - Зелен фон на реда (#d4edda)
  - Зачертаване на името на частта
  - Намалена opacity (0.7)
- Автоматично запазване в споделен/локален режим

#### Бележки към части
- Полета за въвеждане на забележки до всяка част
- Визуална индикация при наличие на бележка
- Синхронизация между устройства

### 5. RAL управление
- Динамична таблица за управление на RAL цветове
- Предефинирани опции за всеки RAL код
- Запазване в споделен режим (userDataCache) или localStorage
- Автоматична миграция от локални към споделени данни

### 6. Интелигентна класификация на части

#### Функция `isFastenerPart()`
Разпознава крепежи по шаблони:
- M8x20, M12x30 (резбови размери)
- Anker части
- Komplekt асемблита
- DIN/ISO стандарти
- Nut, Washer, Bolt, Screw и др.

#### Функция `isFastener()` / `isFastenerAssembly()`
Определя дали асемблито е крепежно

### 7. Данни и структура

#### Поддържани формати
- JSON файлове с версия 2.0
- Структура с `objects` масив
- Всеки обект съдържа:
  - `objectName` - име на възела
  - `sourceFile` - източник
  - `assemblies` - асемблита
  - `flatBOM` - плосък списък с всички елементи
  - `allParts` - всички части

#### Обработка на данни
- Нормализация на имена (`normalizePartName`)
- Превод на заглавия на възли (nodeTranslations)
- Извличане на размер на резба (`extractThreadSize`)
- Дедупликация на елементи при генериране

### 8. Ключови технологии

#### Синхронизация
```javascript
// Запазване на споделени данни
async function saveSharedUserData(data)

// Зареждане на споделени данни
async function loadSharedUserData()

// Запазване на промяна
async function saveUserDataChange(key, value)
```

#### Възстановяване на състояния
```javascript
// Възстановява checkbox-и и бележки
function restoreCheckboxStates()

// Възстановява всички UI състояния
function restoreUserInterfaceStates()

// Зарежда всички потребителски данни
async function loadAllUserData()
```

---

## Дизайн и стилове

### Цветова схема
- Градиенти: `#667eea` → `#764ba2` (фон на body)
- Хедър: `#2c3e50` → `#3498db`
- Успех (зелено): `#28a745`, `#20c997`
- Грешка (червено): `#dc3545`
- Информация (синьо): `#007bff`
- RAL маркировка: `#6f42c1` → `#5a32a3`

### Responsive дизайн
- Grid системи за карти и статистики
- Flex layout за таблици и контейнери
- Адаптивни колони: `repeat(auto-fit, minmax(300px, 1fr))`

---

## Ключови променливи

```javascript
let bomData = null;              // Основни BOM данни
let currentFilter = "";          // Текущ филтър
let isSharedMode = false;        // Дали е в мрежов режим
let serverURL = '';              // URL на сървъра
let userDataCache = {};          // Кеш на потребителски данни
```

---

## Работен процес

1. **Инициализация**
   - `window.load` event → `detectSharedMode()`
   - Проверка за мрежов/локален режим
   - Автоматично извличане на BOM от `/get_bom`

2. **Обработка на данни**
   - `displayBOMData()` → обработка и показване
   - `generateObjectFilter()` → създаване на филтри
   - Генериране на табове (Overview, By Object, Parts Summary и т.н.)

3. **Потребителско взаимодействие**
   - Смяна на табове → `showTab()`
   - Отбелязване на checkbox → `updatePartStatus()`
   - Добавяне на бележка → `saveNote()`
   - Управление на RAL → `updateRalEntry()`, `addNewRalEntry()`, `deleteRalEntry()`

4. **Синхронизация**
   - При промяна → `saveUserDataChange()` → `saveSharedUserData()`
   - При зареждане → `loadSharedUserData()` → `restoreUserInterfaceStates()`

---

## Особености и важни детайли

### Дедупликация на данни
При генериране на Overview таба се използва интелигентна дедупликация:
```javascript
const processedItems = new Set();
const itemKey = `${item.name}|${item.path || ''}|${item.level || 0}`;
if (!processedItems.has(itemKey)) {
    processedItems.add(itemKey);
    // Обработка на елемента
}
```

### Кеширане и refresh
- BOM данните се запазват в localStorage за бърза възстановяване
- При refresh се възстановяват последно заредените данни
- Версия на визуализатора: v2025-11-05B (за контрол на кеша)

### Превод на имена
- Речник `nodeTranslations` за превод на имена на възли от английски на български
- Функция `translateNodeTitle()` за автоматичен превод

---

## Заключение
`unified_bom_viewer.html` е комплексен визуализатор за BOM данни с богата функционалност за управление, филтриране, класификация и синхронизация на информация. Поддържа както локална работа, така и споделен мрежов режим за екипна работа.
